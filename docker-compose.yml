version: '3.8'

services:
  web:
    build: .
    container_name: linkedin-post-generator
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
    restart: always
